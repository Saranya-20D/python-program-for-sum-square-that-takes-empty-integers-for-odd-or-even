def sumsqr(n)
    l=()
    While n<=1
        l=int(input("enter a value")
        l.append(i)
        n=n-1
        even=0
        odd=0
    for p in l:
      if p%2==0:
         even=even+p**2
      else:
         odd=odd+p**2
    K=[odd,even]
    return k,
Print=int(input("enter a number"))
Print(sumsqrt(n))
